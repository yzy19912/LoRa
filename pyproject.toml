[project]
name = "lora-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.14.0",
    "datasets>=5.0.1",
    "peft>=0.20.0",
    "safetensors>=0.8.0",
    "torch>=2.13.0",
    "transformers>=5.15.0",
]
