[project]
name = "lora-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.14.0",
    "datasets>=5.0.1",
    "deepagents>=0.7.9",
    "ipython>=9.16.1",
    "langchain-openrouter>=0.2.8",
    "langgraph>=1.2.11",
    "langsmith>=0.11.1",
    "peft>=0.20.0",
    "pydantic>=2.12.5",
    "safetensors>=0.8.0",
    "torch>=2.13.0",
    "transformers>=5.15.0",
]

[tool.uv.workspace]
members = [
    "hand_write_graph",
]

[dependency-groups]
dev = [
    "ruff>=0.16.5",
]
